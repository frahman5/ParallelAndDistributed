/*
	User defined structures
		- Add explanation and make sure the user only changes types not names!
*/

struct one_work {
    int array[10];
    unsigned long long sz;
};

struct one_result {
    int array[1];
    unsigned long long sz;
    int status;
};